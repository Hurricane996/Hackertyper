Hackertyper 2.0 is a complete rewrite. It now uses ncurses, which makes the code cleaner, as well as make instead of autohell. It has also returned to loading from files, vs compiling code directly from source.
